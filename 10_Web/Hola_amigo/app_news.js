
function app() {

    // 1º Definir los nodos del DOM (la forma de JS de manipular los eventos del DOM) y luego definir los manejadores de eventos
    let  = document.querySelector('#nombre')

    // esta función definirá los manejadores de eventos
}

// document.addEventListener('DOMContentLoaded', app)